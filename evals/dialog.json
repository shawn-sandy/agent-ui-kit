{
  "skill": "dialog",
  "scenarios": [
    {
      "id": "dialog-obvious",
      "kind": "obvious",
      "expect": "dialog",
      "prompt": "In index.html, build a modal dialog that confirms account deletion. It opens from a button in the account settings panel.",
      "baselineFailure": "never chooses where focus lands on open, so the browser focuses the first focusable element - typically the close button, one keystroke from the destructive action",
      "detect": {
        "showModal used": "showModal\\(\\)",
        "chooses initial focus": "\\bautofocus\\b|querySelector\\([^)]*\\)\\.focus\\(\\)",
        "explicit focus restoration": "(previous|last|opener|trigger)[A-Za-z]*\\s*[.=][^;]{0,60}focus\\(\\)|activeElement"
      }
    },
    {
      "id": "dialog-oblique",
      "kind": "oblique",
      "expect": "dialog",
      "prompt": "In index.html I need an overlay panel where the keyboard cannot wander off behind it, Escape gets you out, and when it closes the cursor lands back on whatever opened it.",
      "baselineFailure": "hand-rolls a focus trap over a div with role=dialog instead of using the native element, which means the page behind stays reachable",
      "detect": {
        "showModal used": "showModal\\(\\)",
        "hand-rolled trap over a div": "role=[\\\"']?dialog",
        "explicit focus restoration": "(previous|last|opener|trigger)[A-Za-z]*\\s*[.=][^;]{0,60}focus\\(\\)|activeElement"
      }
    },
    {
      "id": "dialog-adjacent",
      "kind": "adjacent",
      "expect": null,
      "prompt": "In index.html, add a dropdown menu under the avatar in the header, with profile and sign-out items.",
      "baselineFailure": "n/a - this scenario asserts the dialog skill stays quiet",
      "detect": {}
    }
  ]
}

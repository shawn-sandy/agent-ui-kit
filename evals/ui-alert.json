{
  "skill": "ui-alert",
  "scenarios": [
    {
      "id": "ui-alert-obvious",
      "kind": "obvious",
      "expect": "ui-alert",
      "prompt": "In index.html, when the save fails pop up a notification in the corner of the screen that fades away after a few seconds. Screen reader users have to hear it too.",
      "baselineFailure": "builds the notification element at the moment the message arrives, so the live region does not exist when the content is inserted and nothing is announced",
      "detect": {
        "region built at message time": "createElement\\(|insertAdjacentHTML|innerHTML\\s*=\\s*[`'\\\"]\\s*<",
        "live region in static markup": "<(div|p|section)[^>]*role=[\\\"']?(alert|status)",
        "aria-live declared": "aria-live"
      }
    },
    {
      "id": "ui-alert-oblique",
      "kind": "oblique",
      "expect": "ui-alert",
      "prompt": "In index.html, when a background upload finishes the user needs to be told, without moving their focus or interrupting what they are typing.",
      "baselineFailure": "no live region at all, or an assertive one that interrupts the typing the prompt asked to protect",
      "detect": {
        "polite live region": "aria-live=[\\\"']?polite|role=[\\\"']?status",
        "assertive live region": "aria-live=[\\\"']?assertive|role=[\\\"']?alert",
        "moves focus to the message": "\\.focus\\(\\)"
      }
    },
    {
      "id": "ui-alert-adjacent",
      "kind": "adjacent",
      "expect": null,
      "prompt": "In index.html I need something that interrupts the user and blocks the page until they confirm they really want to delete their account.",
      "baselineFailure": "n/a - this scenario asserts the alert skill stays quiet; the dialog is the right answer",
      "detect": {}
    }
  ]
}

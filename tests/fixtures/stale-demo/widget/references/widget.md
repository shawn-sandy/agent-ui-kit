# Widget reference

A deliberately stale fixture. `scripts/check.sh --prove` points the demo generator
at this tree and requires it to report the demo below as out of date. It lives
outside `skills/` so the real gate never sees it.

## Styles

Qualifiers: parts none; variants none; states none.

```css
.auk-widget {
  color: var(--auk-widget-color, #111827);
}
```
